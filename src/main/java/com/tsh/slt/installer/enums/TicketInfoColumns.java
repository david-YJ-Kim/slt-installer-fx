package com.tsh.slt.installer.enums;

public enum TicketInfoColumns {

    ID,
    userId,
    username,
    email,
    grade,
    agentPort,
    satellitePort,
    createdAt,
    expiredAt,
    updatedAt
    ;

}
